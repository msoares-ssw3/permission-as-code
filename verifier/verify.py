"""Verificador standalone de exports (manifest v1).

Requisito de produto: stdlib pura (json + hashlib, zero deps), executável em
máquina limpa com `python verify.py export.json`. Implementação na Sessão 4.
"""

import sys

if __name__ == "__main__":
    sys.exit("verifier ainda não implementado (Sessão 4)")
